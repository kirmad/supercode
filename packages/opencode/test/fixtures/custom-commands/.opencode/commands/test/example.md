---
description: A test command that demonstrates custom commands
argument-hint: <text>
allowed-tools: read, write, edit
output-style: default
---

# Test Custom Command

This is a test custom command with arguments: $ARGUMENTS

It can include shell commands: !`echo "Current date: $(date)"`

And supports file inclusion when needed.

This command is used for testing the custom commands functionality.